# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/monke_say

# API
API_HOST=0.0.0.0
API_PORT=8000
API_SECRET_KEY=your-secret-key-change-in-production

# Frontend
VITE_API_URL=http://localhost:8000

# Claude API
ANTHROPIC_API_KEY=your-anthropic-api-key

# Google Cloud Text-to-Speech (for Hindi audio)
# Option 1: Use service account JSON file (recommended)
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
# Option 2: Use base64-encoded credentials (for containerized environments)
# GOOGLE_TTS_CREDENTIALS=base64-encoded-json-credentials
